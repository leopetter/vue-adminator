import Vue from "vue";
import Vuex from "vuex";

Vue.use(Vuex);

/* CEPTA
export interface IRootState {
  app: IAppState;
  auth: IAuthState;
  grpc: IReplayerState;
}
*/

export default new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
  modules: {}
});
